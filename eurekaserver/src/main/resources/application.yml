spring:
  application:
    name: eurekaserver

  security:
    user:
      name: neo
      password: zero81

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false